## 单行注释

在 Python 中，使用井号(#)来表示单行注释。

例如：

```python
# 这是一个简单的单行注释，用于解释下面代码的功能
print('Hello')
```

## 多行注释（块注释）

虽然 Python 没有专门的多行注释语法，但可以使用三个单引号 (''') 或三个双引号(""")来实现多行注释的效果。

这种方法常用于函数、类或模块开头对功能的详细描述。

例如：

```python
'''
这是一个多行注释的示例。
可以在这里注释多行。
'''
```
