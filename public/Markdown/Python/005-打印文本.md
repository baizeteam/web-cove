print() 函数也可以用来输出多行文本。

可以**使用 \n (换行符) 给文本换行**，也可以使用多个 **print()** 函数输出多行。

例如:

```python
print('Hello world')
print('good\njob')
```

输出

```shell
Hello world
good
job
```

**提示**：每个 print() 函数都会在字符串最后默认添加\n换行
